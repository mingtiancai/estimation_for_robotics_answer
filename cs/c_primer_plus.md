---------------2019—12-28-----------
## 8.4 重定向文件
从命令行指定程序输入输出文件
## 8.5 创建更友好的用户界面  
getchar scanf处理交互时候对换行符的处理逻辑，让交互体验更好
## 8.6 输入验证  
命令交互用户可能不会按照你的提示输入，这时候要尽可能在程序中处理好意外输入的情况。  
## 8.7 菜单浏览  
提供一个菜单，设计程序应对错误输入，在输入正确的时候，在输入数字，给出相应正确处理。

# 9 函数
## 9.1 复习函数  
没啥好说的，就是基础  
## 9.2 ANSI C函数原型 
用一个函数之前把一个函数声明一下。
## 9.3 递归  
自己调用自己
## 9.4 编译多源代码文件程序  
没啥好说的，跟系统有关都有现成工具  
## 9.5 查找地址：&运算符  
c中函数传参是拷贝，地址与原变量地址不同  
## 9.6 改变主调函数中的变量  
使用指针  
## 9.7 指针简介  
基本指针概念  

# 10数组与指针  
## 10.1 初始化数组  
基本数组初始化方法，c99加了一个新功能指定初始化器
## 10.2 多维数组  
数组的数组
## 10.3 指针和数组
数组名就是数组第一个元素首地址
## 10.4 函数/数组和指针
把指针往函数里传，因为数组就是指针，可以直接传数组名
## 10.5 指针操作
各种基本指针运算
## 10.6 保护数组中元素
const用法，跟c++中基本用法一致

--------------2019-12-29------------------
## 10.7 指针和多维数组
注意多维数组的本质就可以清楚的处理数组跟指针的关系
## 10.8 变长数组
数组的长度可以使用变量来设计，但是我用微软的编译器并没有允许使用该特性，gcc可以使用该特性。
## 10.9 复合字面量
类似于C++中的字面值常量，具有块作用域，生存在最小的{}范围内！

# 11字符串字符串函数
## 11.1表示字符串和字符串I/O
字符串就是字符数组最后加一个'\0',但是要注意使用字符数组表示字符串跟使用指针表示字符串的差异。
## 11.2字符串输入
gets(),fgets(),gets_s(),scanf()四个函数的优势和缺陷，gets()缺陷太大，scanf()更适合截取输入单词,gets_s()新的标准，总体fgets()比较稳妥
## 11.3字符串输出
puts(),fputs(),printf()
puts()会在字符串末尾加上换行符
fputs()是puts()的文件版本，后面需要跟一个文件或者stdout
## 11.4自定义输入输出函数
自己写输入输出函数，就是用指针去处理数据
## 11.5字符串函数
strlen()返回字符串大小不包括'\0'
strcat()拼接字符串
strncat()拼接部分字符串
strcmp()逐字符比较字符串
strncmp()选择前n字符比较字符串
strcopy()将源字符串拷贝到目的字符串
strncopy()将源字符串的n个元素拷贝到目的字符串
sprintf()将多个字符串组合成新的字符串
strchr()在字符串中找字符
## 11.6字符串排序
一个使用选择排序的案例

--------------2019-12-30------------------

## 11.7 ctype.h字符函数和字符串
ctype.h中包含大量用于处理字符的函数，因为字符串可以看作以'\0'结尾的字符数组，所以用指针和这些函数结合组合出不同字符串处理功能！
## 11.8 命令行参数
argc，argv
## 11.9 把字符串转化为数字
atoi()字符串转化为整数，类似的atol(),atod();
更智能的函数：strtol()...

# 12.存储类别，链接和内存管理
## 12.1存储类别
作用域：块作用域，函数作用域，文件作用域，全局变量；
链接：内部链接，外部链接，无链接；
存储器：静态存储期，线程存储期，自动存储期，动态分配存储期；
函数也可以有本文件内作用域还是被其他文件可调用作用域
## 12.2随机数函数和静态变量
自己写一个简单的伪随机数生成函数，跟结果跟随机种子有关，相同的种子产生的随机序列相同。
## 12.3掷骰子
用rand()函数设置一个六面骰子，rand()返回的数字本身就是均匀分布的，因此不断%6，把数字压缩到1-6就可以
## 12.4分配内存malloc()和free()
动态存储
malloc(),free(),calloc()
## 12.5 ANSI C限定符
const volatile,restrict,_Atomic

# 13文件输入/输出
## 13.1 与文件进行通信
文本模式和二进制模式

---------------2019-12-31-----------------
## 13.2 标准I/O
FILE结构；fopen(),getc(),putc(),EOF,fclose()
## 13.3 一个简单文件压缩例子
可以改文件后缀名
## 13.4 文件I/O:fprintf(),fscanf(),fgets(),fputs()
fprintf(),fscanf(),fgets(),fputs()
## 13.5 随机访问：fseek(),ftell()
fseek()定位文件指针所指向的文件的位置
ftell()确定文件指针所指文件的当前位置到文件起点的字节数
## 13.6 标准I/O机理
操作系统底层I/O的机理
## 13.7 其他标准I/O函数
ungetc()将指定字符放回输入流中
fflush()刷新缓冲区
setvbuf()创建一个供标准I/O函数使用的缓冲区
fread(),fwrite()---二进制I/O
feof()检验是否碰到文件EOF
ferror()检验文件读写是否错误

# 14 结构和其他数据形式
## 14.1 示例问题：创建图书目录
一个使用结构的示例
## 14.2 建立结构声明
 结构体声明
## 14.3 定义结构变量
初始化结构体
访问结构成员
结构初始化器
## 14.4 结构数组
把结构作为数组的基本元素
## 14.5 嵌套结构
结构中套结构
## 14.6 指向结构的指针
申请以一个指向改结构体类型的指针，从这种结构的一个变量取地址就好了
## 14.7 向函数传递结构的信息
向函数传递结构成员，地址，结构本身，结构指针
复合字面量和结构
伸缩型数据成员
匿名结构
## 14.8 把结构内容保存到文件中
本质上跟其他没有区别

-------------2020-1-1----------------------
## 14.9 链式结构
数据结构中的链式结构
## 14.10 联合简介
联合里面有很多数据类型，一次只能用其中的一个，存储按照里面最大的需要的存储空间设置存储空间
匿名联合
## 14.11 枚举类型
enum
## 14.12 typedef简介
现有类型的别名
## 14.13 其他复杂的声明
指针，函数，数组之间的互相组合，包含指针的数组，指向数组的指针。。。
## 14.14 函数和指针
函数指针，只需在正常声明的函数，用(*fp)代替函数名就可以

# 15 位操作
## 15.1 二进制数，位和字节
基本的二进制定义
## 15.2 其他进制数
八进制
十六进制
## 15.3 C按位运算符
与/或/非/异或
掩码
左移/右移
## 15.4 位字段
通过结构声明每个字段的标签和字段宽度
## 15.5 对齐特性
如何安排对象在内存中的位置

# 16 C预处理器和C库
## 16.1 翻译程序第一步
将源代码进行字符转换

----------------2020_1_2-------------------

## 16.2 明示变量：#define
定义宏
## 16.3 在#define中使用参数
类函数宏---跟函数的区别
#运算符
##运算符
变参宏函数，... __VA_ARGS__
## 16.4 宏和函数的选择
宏函数有使用风险，但是可以提高执行速度
## 16.5 文件包含：#include
所有特性都使用过
## 13.6 其他指令
#undef
#ifdef
#else
#endif
#ifndef
#elif
预定义宏：__DATE__,__FILE__,__LINE__,__STDC__,__STDC_HOSTED__,__STDC__VERSION__,__TIME__,__func__
#line
#error
#pragma
## 16.7 内联函数
在使用内联函数时直接将内联代码展开到调用部分
## 16.8 _Noreture函数
_Noreture函数跟inline一样，调用不会产生函数返回，void产生返回，但是没有返回值，二者本质不同
## 16.9 C库
没啥东西
## 16.10 数学库
C语言也提供了一些泛型机制
## 16.11 通用工具库
atexit()
exit()
qsort()
## 16.12 断言库
assert()
_Static_assert()---可在编译期计算
## 16.13 string.h库中的memcpy()和memmove()
memcpy()
memmove()
## 16.14 可变参数：stdarg.h
va_list()
va_start()
va_arg()
va_end()
va_copy()

# 17 高级数据表示








