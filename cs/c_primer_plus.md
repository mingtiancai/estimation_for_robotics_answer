---------------2019—12-28-----------
## 8.4 重定向文件
从命令行指定程序输入输出文件
## 8.5 创建更友好的用户界面  
getchar scanf处理交互时候对换行符的处理逻辑，让交互体验更好
## 8.6 输入验证  
命令交互用户可能不会按照你的提示输入，这时候要尽可能在程序中处理好意外输入的情况。  
## 8.7 菜单浏览  
提供一个菜单，设计程序应对错误输入，在输入正确的时候，在输入数字，给出相应正确处理。

# 9 函数
## 9.1 复习函数  
没啥好说的，就是基础  
## 9.2 ANSI C函数原型 
用一个函数之前把一个函数声明一下。
## 9.3 递归  
自己调用自己
## 9.4 编译多源代码文件程序  
没啥好说的，跟系统有关都有现成工具  
## 9.5 查找地址：&运算符  
c中函数传参是拷贝，地址与原变量地址不同  
## 9.6 改变主调函数中的变量  
使用指针  
## 9.7 指针简介  
基本指针概念  

# 10数组与指针  
## 10.1 初始化数组  
基本数组初始化方法，c99加了一个新功能指定初始化器
## 10.2 多维数组  
数组的数组
## 10.3 指针和数组
数组名就是数组第一个元素首地址
## 10.4 函数/数组和指针
把指针往函数里传，因为数组就是指针，可以直接传数组名
## 10.5 指针操作
各种基本指针运算
## 10.6 保护数组中元素
const用法，跟c++中基本用法一致

--------------2019-12-29------------------
## 10.7 指针和多维数组
注意多维数组的本质就可以清楚的处理数组跟指针的关系
## 10.8 变长数组
数组的长度可以使用变量来设计，但是我用微软的编译器并没有允许使用该特性，gcc可以使用该特性。
## 10.9 复合字面量
类似于C++中的字面值常量，具有块作用域，生存在最小的{}范围内！

# 11字符串字符串函数
## 11.1表示字符串和字符串I/O
字符串就是字符数组最后加一个'\0',但是要注意使用字符数组表示字符串跟使用指针表示字符串的差异。
## 11.2字符串输入
gets(),fgets(),gets_s(),scanf()四个函数的优势和缺陷，gets()缺陷太大，scanf()更适合截取输入单词,gets_s()新的标准，总体fgets()比较稳妥
## 11.3字符串输出
puts(),fputs(),printf()
puts()会在字符串末尾加上换行符
fputs()是puts()的文件版本，后面需要跟一个文件或者stdout
## 11.4自定义输入输出函数
自己写输入输出函数，就是用指针去处理数据
## 11.5字符串函数
strlen()返回字符串大小不包括'\0'
strcat()拼接字符串
strncat()拼接部分字符串
strcmp()逐字符比较字符串
strncmp()选择前n字符比较字符串
strcopy()将源字符串拷贝到目的字符串
strncopy()将源字符串的n个元素拷贝到目的字符串
sprintf()将多个字符串组合成新的字符串
strchr()在字符串中找字符
## 11.6字符串排序
一个使用选择排序的案例

--------------2019-12-30------------------

## 11.7 ctype.h字符函数和字符串
ctype.h中包含大量用于处理字符的函数，因为字符串可以看作以'\0'结尾的字符数组，所以用指针和这些函数结合组合出不同字符串处理功能！
## 11.8 命令行参数
argc，argv
## 11.9 把字符串转化为数字
atoi()字符串转化为整数，类似的atol(),atod();
更智能的函数：strtol()...

# 12.存储类别，链接和内存管理
## 12.1存储类别
作用域：块作用域，函数作用域，文件作用域，全局变量；
链接：内部链接，外部链接，无链接；
存储器：静态存储期，线程存储期，自动存储期，动态分配存储期；
函数也可以有本文件内作用域还是被其他文件可调用作用域
## 12.2随机数函数和静态变量
自己写一个简单的伪随机数生成函数，跟结果跟随机种子有关，相同的种子产生的随机序列相同。
## 12.3掷骰子
用rand()函数设置一个六面骰子，rand()返回的数字本身就是均匀分布的，因此不断%6，把数字压缩到1-6就可以
## 12.4分配内存malloc()和free()
动态存储
malloc(),free(),calloc()
## 12.5 ANSI C限定符
const volatile,restrict,_Atomic

# 13文件输入/输出
## 13.1 与文件进行通信
文本模式和二进制模式

---------------2019-12-31-----------------
## 13.2 标准I/O
FILE结构；fopen(),getc(),putc(),EOF,fclose()
## 13.3
























